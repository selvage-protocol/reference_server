# `vectors/` is vendored test data, not source. `vectors/fixture/keys.json` fixes a
# room key, a host key and the session keypairs the vectors seal against, and every
# peer vector carries the keys its frames were sealed with. They are public by
# construction: the specification publishes them, and its fixture says how they are
# reproduced. The same reason the corpus is excluded from the large-file guard.
vectors/
